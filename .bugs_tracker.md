# Bug Tracker & Pattern Memory

**Last Updated:** March 11, 2026
**Active Bugs:** 1

---

## 🚨 Active Bugs
### BUGS-004: Container Crash on Startup
**Status:** Active | **Severity:** Critical (Blocker)
**Description:** The `ui-1` container crashes immediately on startup, leading to "Unable to connect" in the browser.
**History:** This bug has presented with multiple symptoms, which we have been resolving one by one.
  - ~~`ModuleNotFoundError: 'app'`~~ (Fixed by setting `PYTHONPATH`)
  - ~~`ValueError: Extra inputs not permitted`~~ (Fixed by Pydantic `extra='ignore'`)
  - ~~`ModuleNotFoundError: 'msal'`~~ (Fixed by adding to `pyproject.toml`)
  - ~~`ImportError: Circular Import`~~ (Fixed by refactoring `pm_tools/__init__.py`)
  - ~~`OperationalError: could not translate host name "db"`~~ (Fixed by updating `DATABASE_URL` and Docker healthcheck)
**Current Hypothesis:** The application is now crashing due to a missing or invalid environment variable during component initialization (e.g., `OPENAI_API_KEY` is missing, causing `LLMFactory` to raise a `ValueError`).
**Next Step:** Get definitive crash log using `docker-compose down` followed by `docker-compose up`.

---

## 🔍 Bug Patterns Identified (AI-UPDATED)
*(AI: Read this before writing new integration code to avoid repeating mistakes)*

### Pattern 1: Sidecar Race Conditions
**Occurrences:** 2 times (MCP filesystem + Database)
**Root cause:** Containers starting before services are ready
**Solution:** Always use exponential backoff decorators on connection logic and Docker healthchecks.

### Pattern 2: API Rate Limiting
**Occurrences:** 1 time (External Search Tool)
**Root cause:** Parallel agent execution triggered 429 errors.
**Solution:** Implemented the Token Bucket algorithm in the `RateLimiter` middleware.

### Pattern 3: Docker Import Paths
**Occurrences:** 1 time (Streamlit UI)
**Root cause:** Running scripts from subdirectories in Docker breaks absolute imports if `PYTHONPATH` is not set.
**Solution:** Always set `ENV PYTHONPATH=/app` (or workspace root) in Dockerfiles for Python projects.

### Pattern 4: Undeclared Dependencies
**Occurrences:** 1 time (`msal` for Planner)
**Root cause:** A dependency was used in the code but not declared in `pyproject.toml`.
**Solution:** All `import` statements for third-party libraries must have a corresponding entry in `pyproject.toml`.

### Pattern 5: Circular Imports
**Occurrences:** 1 time (`PMToolFactory` and `pm_tools`)
**Root cause:** A lower-level package (`pm_tools`) should never import a higher-level abstraction (`factories`).
**Solution:** Ensure imports follow a single direction of dependency: App -> Factories -> Adapters -> Packages.
